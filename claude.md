# Smithタイル描画アプリ仕様書

## 概要
非周期モノタイルであるSmithタイルを描画するWebアプリケーション。学習用途のため可読性を重視し、タイルクラスは再利用可能なモジュールとして設計する。

## 技術仕様
- **描画方式**: HTML5 Canvas
- **開発環境**: Vite + TypeScript
- **配布形式**: 静的サイト
- **対象ブラウザ**: モダンブラウザ

## 機能要件

### 基本機能
1. **Smithタイル描画**: 14個の頂点を持つ多角形の描画
2. **比率選択**: 4種類の辺の比率（1:4, 1:√3, √3:1, 4:1）の選択
3. **サイズ調整**: スライダーによる描画サイズの調整
4. **座標表示**: 各頂点の座標値をリスト表示

### UI仕様
- **レイアウト**: 左側にCanvas、右側に操作パネル
- **操作パネル内容**:
  - 辺の比率選択（ラジオボタンまたはセレクトボックス）
  - サイズ調整スライダー
  - 頂点座標一覧

## Smithタイルの仕様

### 幾何学的特性
- **頂点数**: 14個
- **辺の種類**: 2種類の長さ（短辺と長辺）
- **角度**: 90度と120度に関係する値のみ
- **内角の値**（頂点Aから反時計回り）:
  90°, 120°, 180°, 120°, 270°, 120°, 90°, 120°, 270°, 120°, 90°, 240°, 90°, 240°

### 辺の比率パターン
1. **1:4** - 短辺:長辺 = 1:4
2. **1:√3** - 短辺:長辺 = 1:√3
3. **√3:1** - 短辺:長辺 = √3:1  
4. **4:1** - 短辺:長辺 = 4:1

### 座標系
- **基準点**: 頂点A（図V-④参照）を原点(0, 0)に配置
- **初期向き**: 特別な制約なし
- **座標精度**: 小数点以下2桁まで表示

## ファイル構成
```
project/
├── src/
│   ├── SmithTile.ts          # タイルクラス（再利用可能モジュール）
│   ├── main.ts               # メインアプリケーション
│   └── style.css             # スタイルシート
├── index.html                # HTMLファイル
├── vite.config.ts            # Vite設定
├── package.json              # パッケージ設定
└── tsconfig.json             # TypeScript設定
```

## SmithTileクラス仕様

### コンストラクタ
```typescript
constructor(ratio: RatioType, baseLength: number = 10)
```

### パブリックメソッド
- `getVertices(): Point[]` - 全頂点座標を取得
- `setRatio(ratio: RatioType): void` - 辺の比率を設定
- `setBaseLength(length: number): void` - 基準長を設定
- `draw(ctx: CanvasRenderingContext2D, offsetX: number = 0, offsetY: number = 0): void` - Canvasに描画（辺線と頂点点の両方）

### 型定義
```typescript
type RatioType = '1:4' | '1:√3' | '√3:1' | '4:1'
interface Point {
  x: number
  y: number
}
```

## 描画仕様
- **基準長**: デフォルト10px（スライダーで調整可能）
- **線の色**: 黒（#000000）
- **線の太さ**: 2px
- **背景色**: 白（#ffffff）
- **Canvas サイズ**: 800x600px
- **頂点の描画**: 各頂点を円形の点で強調表示
- **頂点の色**: 赤（#ff0000）
- **頂点の半径**: 3px

## 操作パネル仕様

### 比率選択
- **UI要素**: ラジオボタン
- **選択肢**: 1:4, 1:√3, √3:1, 4:1
- **デフォルト**: 1:4

### サイズスライダー
- **範囲**: 5px - 50px
- **ステップ**: 1px
- **デフォルト**: 10px

### 座標表示
- **形式**: リスト形式
- **表示内容**: 頂点番号（0-13）と座標(x, y)
- **精度**: 小数点以下2桁
- **例**: `頂点0: (0.00, 0.00)`

## 開発時の注意事項
1. **可読性重視**: コメント、変数名、関数名を分かりやすくする
2. **モジュール設計**: SmithTileクラスは他のプロジェクトでも利用可能にする
3. **型安全性**: TypeScriptの型システムを活用する
4. **座標計算**: 与えられた角度情報から頂点座標を自動計算する
5. **エラーハンドリング**: 不正な入力値に対する適切な処理

## 将来的な拡張性
- タイルクラスは他のアプリケーションでも利用可能
- 頂点情報のエクスポート機能追加に対応可能
- 描画スタイルのカスタマイズ追加に対応可能